{
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "markdown-it": "^14.3.0",
    "wrangler": "^4.114.0"
  },
  "type": "module",
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve",
    "deploy": "npm run build && wrangler deploy",
    "preview": "npm run build && wrangler dev"
  }
}
